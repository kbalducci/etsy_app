<!-- name, a price, a list of its materials, and a link back to the original listing. -->
<h1>Etsy With Ease</h1>
<%= form_tag('/', method: 'get') do %>
  <%= search_field_tag('query') %>
  <%= submit_tag("Keyword Search") %>
<% end %>
<ul>
  <% @items.each do |item| %>
    <li>Item Name: <%= item["title"] %><br />
        <p>Description: <%= item["description"] %></p><br />
        <div class='price'>Price: $<%= item["price"] %></div><br />
        <div class='materials'>Materials: <%= item["materials"].to_s %></div><br />
        <div class='link'><a href='<%= item["url"] %>' > Item url </a></div>

<!--       <img src='<%= item["thumbnailImage"] %>' />
      <div class='price'>
        Price: <strong>$<%= item["salePrice"] %></strong>
        <%= "$#{item["msrp"]}" if item ["msrp"] %>
        <% if item["salePrice"].to_i < item["msrp"].to_i %>
          <br/><small>Wow! What savings~!</small>
          <% end %>
      </div>
      <p><%= item["shortDescription"] %></p> -->
    </li>
    <% end %>
</ul>
